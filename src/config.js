// Заявки с сайта уходят в Telegram через серверные функции Netlify
// (netlify/functions/booking.js и netlify/functions/newsletter.js).
// Токен бота и chat_id хранятся в переменных окружения Netlify
// (Site settings → Environment variables), а не в этом файле — так их
// не увидит никто из посетителей сайта.
//
// Пока сайт не задеплоен на Netlify или переменные окружения не настроены,
// оставьте DEMO_MODE = true — формы будут показывать "Спасибо", но никуда
// ничего не отправлять. Подробности — в DEPLOY.md.
export const DEMO_MODE = false

// Время, которое можно выбрать при бронировании стола.
// Функция booking.js сама следит, сколько столов уже занято на каждый слот
// (через Netlify Blobs — это не отдельная база данных, а встроенное
// хранилище прямо внутри serverless-функции, ничего дополнительно поднимать
// не нужно).
export const TIME_SLOTS = ['10:00', '12:00', '14:00', '16:00', '18:00', '20:00']

